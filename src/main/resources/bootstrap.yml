spring:
  application:
    name: reservationservice
  cloud:
    config:
      discovery:
        enabled: true
        serviceId: CONFIGSERVER

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8765/eureka